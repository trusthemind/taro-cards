/**
 * Decorative parallax star layer. Purely presentational and inert to the
 * pointer; the two drifting sub-layers are defined in `globals.css`.
 */
export function Starfield() {
  return <div className="starfield" aria-hidden="true" />
}
